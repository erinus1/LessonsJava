package main.Pattern_10Module;

import org.openqa.selenium.WebDriver;
import org.openqa.selenium.WebElement;
import org.openqa.selenium.interactions.Actions;
import org.openqa.selenium.support.FindBy;
import org.openqa.selenium.support.PageFactory;

/*
public class DressPage {

    @FindBy(xpath="//*[@id=\"block_top_menu\"]/ul/li[1]/a")
    private WebElement womanButton;

    @FindBy(xpath = "//*[@id=\"block_top_menu\"]/ul/li[1]/ul/li[2]/a")
    private WebElement dressButton;

    public DeveloperApplyPage(WebDriver driver){
        this.driver=driver;

        //Initialise Elements
        PageFactory.initElements(driver, this);
    }


    //hover and find Dresses at category list
    public DressPage hover() {
        Actions action = new Actions(driver);
        womanButton.click();
        waitForVisibility(dressButton);
        action.moveToElement(dressButton).click().build().perform();
        return new DressCategory(driver);
    }

}

 */
